// The module name determines the package and class name of the
// generated Java config class: `samples.boot.AppConfig`.
module samples.boot.AppConfig

server: Server

class Server {
  endpoints: Listing<Endpoint>
}

class Endpoint {
  name: String
  port: UInt16
}
